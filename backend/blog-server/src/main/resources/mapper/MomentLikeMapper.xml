<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.hazenix.mapper.MomentLikeMapper">

    <insert id="insert">
        INSERT INTO moment_like (article_id, ip, create_time)
        VALUES (#{articleId}, #{ip}, #{createTime})
    </insert>

    <select id="getByArticleIdAndIp" resultType="top.hazenix.entity.MomentLike">
        SELECT * FROM moment_like WHERE article_id = #{articleId} AND ip = #{ip}
    </select>

</mapper>
