<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.hazenix.mapper.UserArticleMapper">
    <insert id="insert">
        insert into user_article(user_id, article_id, is_like, is_favorite) values (#{userId}, #{articleId}, #{isLike}, #{isFavorite})
    </insert>
    <update id="update">
        update user_article
        <set>
            <if test="isLike != null">
                is_like = #{isLike},
            </if>
            <if test="isFavorite != null">
                is_favorite = #{isFavorite},
            </if>
        </set>
        where user_id = #{userId} and article_id = #{articleId}
    </update>


    <select id="getByUserIdAndArticleId" resultType="top.hazenix.entity.UserArticle">
        select * from user_article where user_id = #{userId} and article_id = #{articleId}
    </select>
</mapper>
