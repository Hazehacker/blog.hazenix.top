<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.hazenix.mapper.UserArticleMapper">
    <insert id="insert">
        insert into user_article(user_id, article_id, is_liked, is_favorite) values (#{userId}, #{articleId}, #{isLiked}, #{isFavorite})
    </insert>
    <update id="update">
        update user_article
        <set>
            <if test="isLike != null">
                is_liked = #{isLiked},
            </if>
            <if test="isFavorite != null">
                is_favorite = #{isFavorite},
            </if>
        </set>
        where user_id = #{userId} and article_id = #{articleId}
    </update>


    <select id="getByUserIdAndArticleId" resultType="top.hazenix.entity.UserArticle">
        select * from user_article where user_id = #{userId} and article_id = #{articleId}
    </select>
    <select id="getFavoriteCount" resultType="java.lang.Integer">
        select count(*) from user_article where is_favorite = 1 and user_id = #{userId}
    </select>
    <select id="getLikeCount" resultType="java.lang.Integer">
        select count(*) from user_article where is_liked = 1 and user_id = #{userId}
    </select>
</mapper>
