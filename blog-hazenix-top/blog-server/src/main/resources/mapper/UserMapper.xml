<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.hazenix.mapper.UserMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user
        ( username, password, phone, email, refresh_token, avatar, gender, status, role, create_time, update_time, last_login_time)
        values
        ( #{username}, #{password}, #{phone}, #{email}, #{refreshToken}, #{avatar}, #{gender}, #{status}, #{role}, #{createTime}, #{updateTime}, #{lastLoginTime})
    </insert>
    <update id="update">
        update user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="role != null">
                role = #{role},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="lastLoginTime != null">
                last_login_time = #{lastLoginTime},
            </if>
        </set>
        where id = #{id}
    </update>
    <update id="updateLastLoginTime">
        update user
        <set>
            <if test="lastLoginTime != null">
                last_login_time = #{lastLoginTime},
            </if>
        </set>
        where id = #{id}
    </update>


    <select id="getById" resultType="top.hazenix.entity.User">
        select * from user where id = #{id}
    </select>
    <select id="selectByEmail" resultType="top.hazenix.entity.User">
        select * from user where email = #{email}
    </select>
</mapper>
